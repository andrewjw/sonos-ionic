language: node_js

node_js:
  - "node"

before_install:
  - sudo apt-get install -y libsecret-1-dev

install:
  - npm install -g gulp-cli
  - npm ci

script:
  - gulp build
  - gulp cover

addons:
  apt:
    update: true
